require "spec_helper"

RSpec.describe Ruby::Cannapigeon do
  it "has a version number" do
    expect(Ruby::Cannapigeon::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
